# 디자인 감사 100항 — 판정 및 실행 기획 (2026-09-20)

랜딩(nailspo.vercel.app)과 코드 전수 검토 결과. 감사 목록 100개를 하나씩 판정했다.

판정 기준 4종:
- **수정** — 이번 커밋에서 코드로 해결
- **보류** — 해결 방향은 확정하되 지금 하지 않음 (사유 명기, 대부분 검증 게이트 결과 대기)
- **이미 해결** — 감사 시점에 이미 코드에 대응이 있었음 (스크린샷만 보고 낸 오판)
- **오진** — 감사 항목 자체가 틀렸음 (정정 내용 명기)

## 요약

| 판정 | 개수 |
|---|---|
| 수정 (이번 커밋) | 31 |
| 보류 | 38 |
| 이미 해결 | 22 |
| 오진 | 9 |

핵심 원칙: **검증 트랙(방문 200 · 가격 클릭률 3%)이 끝나기 전에는 판정 데이터를 오염시키는 변경을 하지 않는다.** 무드 전면 개편·섹션 재배치처럼 전환율 자체를 흔드는 변경은 전부 보류다. 이번 커밋은 "거짓말 제거(정직성)·깨진 배관(공유/SEO)·닫힌 문 앞의 탈출구(퍼널)"만 고친다.

---

## A. 버그·설정 (1~6)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 1 | og:image가 localhost | **수정** | ① `layout.tsx` SITE_URL 폴백에 `VERCEL_PROJECT_PRODUCTION_URL` 추가 ② Vercel에 `NEXT_PUBLIC_SITE_URL=https://nailspo.vercel.app` 등록. 재배포 시 반영 |
| 2 | /api/designs 0건 | **보류** | 갤러리는 하드코딩 폴백으로 정상 동작. 라이브 파이프라인 채우기는 콘텐츠 작업 (검수 통과작 축적 후) |
| 3 | alt 누락 6건 | **오진** | 6건 전부 장식 이미지(hand, hand-after, 구슬 4개)의 의도적 `alt=""` + 무대 전체 `role=img aria-label`. 올바른 패턴 |
| 4 | reveal 미노출 시 빈 화면 | **이미 해결** | `useReveal`이 `.js-reveal-ready` 게이트로 no-JS/IO 미지원/인쇄 폴백 처리. 풀페이지 캡처 도구만 못 잡는 것 |
| 5 | og.jpg 실존 미검증 | **이미 해결** | `public/og.jpg` 존재 확인 |
| 6 | themeColor 색 불일치 | **보류** | #5aa9e6 vs 히어로 상단 실측 차이 미미. 무드 개편(H그룹) 때 함께 |

## B. 전환 퍼널 (7~16)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 7 | "무료" CTA 누르면 초대 벽 | **수정** | 히어로 CTA를 게이트 상태 분기: 게이트 닫힘 → `초대 코드로 시작하기` |
| 8 | 게이트 중 CTA 문구 거짓 | **수정** | 7과 동일 처리 (TopBar·FooterCta 포함 3곳) |
| 9 | 상단바·히어로 CTA 중복 | **보류** | 중복 자체는 랜딩 관례(스크롤 후 상단바 CTA만 남음). 문구만 정직하게 고치고 구조는 유지 |
| 10 | 게이트 방문자의 전환 경로 은닉 | **수정** | 게이트 카드에 구독 알림 탈출구 링크 추가 (12와 동일 건) |
| 11 | 히어로 필 클릭 불가 | **보류** | 필은 상태 표시 장치. 링크화하면 CTA와 경쟁. 12번 탈출구로 대체 |
| 12 | 게이트 카드에 대체 행동 없음 | **수정** | 게이트 폼 아래 `초대가 없다면 — 열릴 때 알림 받기 ↓` → `#subscribe` 앵커 |
| 13 | 가격 버튼 정체 노출 시 신뢰 손상 | **이미 해결** | 클릭 즉시 "아직 준비 중" 공개 + 이메일 미저장. 정직성 규칙 준수 확인 |
| 14 | FAQ→구독 되돌아 스크롤 | **수정** | PriceSection에 `id="subscribe"` 부여 (12의 앵커 대상). FAQ 본문 링크화는 content.ts가 평문이라 보류 |
| 15 | 이메일 수집 지점 없음 | **오진** | PriceProbe 2단계(클릭→이메일 폼) 존재 |
| 16 | 푸터 CTA 대안 제안 없음 | **수정** | FooterCta도 게이트 분기 + 게이트 시 구독 보조 링크 |

## C. 히어로 (17~26)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 17 | 네일 4개만 칠해짐 | **보류** | `hand-after.webp` 재생성 필요 — 이미지 에셋 작업. 다음 에셋 배치 때 |
| 18 | 엄지·하단 잘림 | **보류** | 17과 동일 에셋 건 |
| 19 | 손·배경 색온도 불일치 | **보류** | 17과 동일 에셋 건 |
| 20 | 히어로 네일과 갤러리 톤 불일치 | **보류** | 17과 동일 에셋 건 |
| 21 | "SCROLL ↓" 영어 | **수정** | `아래로 ↓` 한국어화 |
| 22 | '이달의 네일' 개념 미설명 | **보류** | 헤드라인 교체는 전환율 변수 — 검증 트랙 중 카피 동결 |
| 23 | 서브카피가 기능 나열 | **보류** | 22와 동일 사유 |
| 24 | 히어로 높이 과함 | **보류** | 모핑 무대와 1:1 결합("구조 변경 금지" 주석). 무드 개편 때 |
| 25 | SEPTEMBER 영어 표기 | **보류** | `useIssue().monthLabel` 발행호 컨셉의 일부. 무드 개편 때 일괄 |
| 26 | 신뢰 신호 제로 | **보류** | content.ts 정직성 규칙("없는 실적을 지어내지 않는다")과 충돌 — 실데이터 쌓인 뒤 |

## D. 초대 게이트/툴 카드 (27~33)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 27 | 카드 텍스트 밀도 과함 | **보류** | assurance 3문단은 법적·정직성 고지. 축약은 privacy 문구와 함께 검토 |
| 28 | 확인/구글 로그인 순서 불명확 | **보류** | 게이트 UX 개편은 초대 코드 운영 방식 확정 후 |
| 29 | 카드 내부 패딩 답답 | **보류** | 소소한 CSS 취향 건 — 무드 개편 때 |
| 30 | 코드 실패 UX 미지수 | **이미 해결** | "초대 코드가 맞지 않아요" 인라인 에러 존재 (`page.tsx:352`) |
| 31 | 요구가 이유보다 먼저 | **이미 해결** | 라벨("지금은 초대받은 분만…")이 입력보다 먼저 옴 |
| 32 | 탈출구 링크 부재 | **수정** | 12와 동일 건 |
| 33 | 유일한 다크 밴드 | **보류** | 배경 리듬은 Testimonials 주석에 설계 근거 있음. 무드 개편 때 |

## E. 갤러리 (34~40)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 34 | 착용샷 아닌 팁 제품컷 | **보류** | 실제 착용샷 생성물 축적 후 교체 (콘텐츠 작업) |
| 35 | 4장 vs "5종" 부조화 | **보류** | 갤러리 카피는 5를 주장하지 않음 — 5번째 검수 통과작 추가 시 자연 해소 |
| 36 | 기울기 4장이라 흐트러짐 | **수정** | 모바일 1열에서 tilt 0 처리 (82와 함께) |
| 37 | 카드 확대 보기 없음 | **보류** | 라이트박스는 신규 기능 — 검증 후 |
| 38 | 입력→출력 쌍 부재 | **보류** | 콘텐츠 작업. "영감 사진은 섞지 않았어요" 원칙과 양립하는 별도 섹션 필요 |
| 39 | 라이브 데이터 품질 게이트 | **보류** | /api/designs 연동 시점에 검수 통과작만 싣는 규칙 코드화 |
| 40 | 이미지 배경 무드 충돌 | **보류** | 34 교체 시 함께 |

## F. 가격/구독 (41~45)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 41 | 받는 게 뭔지 불명확 | **이미 해결** | PriceProbe 본문에 "번호·가격·예약 문구까지 붙은 세트와 인스타 게시물을 매달 25일에" 명시 |
| 42 | 문구 중복 서술 | **오진** | 헤드(질문)와 본문(내용물 설명)은 역할이 다름 |
| 43 | 무료/유료 경계 불명 | **수정** | PriceProbe에 한 줄 추가: "시안 만들기는 계속 무료예요. 구독은 완성 세트를 받아보는 선택이에요." |
| 44 | 가격이 제품 설명보다 먼저 | **보류** | PriceSection 주석에 배치 근거(측정 장치 도달률) 있음 — 검증 설계의 일부라 이동 금지 |
| 45 | 클릭 후 피드백 미검증 | **이미 해결** | done 상태 + 접수 확인 문구 존재 |

## G. 카피·메시지 (46~54)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 46 | '이달아' 이름 미해설 | **이미 해결** | 푸터 "이달아 — AI가 만드는 이달의 네일". 추가 해설은 카피 동결 대상 |
| 47 | 내부 용어 밀도 | **보류** | 카피 동결 (22 사유) |
| 48 | '호' 개념 3회 반복 | **보류** | 카피 동결 |
| 49 | 섹션 필 영어 라벨 | **수정** | 시각 유지, 스크린리더에서만 숨김(91과 동일 건). 한국어 교체는 무드 개편 때 |
| 50 | 좋은 카피가 중반에 묻힘 | **보류** | 헤드라인 교체는 카피 동결 |
| 51 | 예상 장면 3개 전부 해피패스 | **보류** | 실후기 확보 전 한계 — 정직 표기는 이미 돼 있음 |
| 52 | "매월"이 숫자 카드 포맷에 어색 | **보류** | 취향 수준 — 무드 개편 때 |
| 53 | "아쉬운 컷" 맥락 없음 | **보류** | 카피 동결 |
| 54 | 저장 안 됨 vs 보관함 기능 | **오진** | 보관함은 로그인 사용자 전용(`accountEmail && <Library/>`), FAQ는 비로그인 기준 설명. 단 FAQ에 "로그인하면 보관함에 저장돼요" 한 줄이 빠진 건 사실 → **수정**으로 승격: FAQ 답변 보강 |

## H. 비주얼 아이덴티티 (55~62) — 전체 보류

55~62 전부 **보류**. XP 초원 무드가 타깃과 맞느냐는 이 페이지 최대의 디자인 결단이지만, 지금 바꾸면 검증 트랙(가격 클릭률 3%) 판정이 "무드 때문인지 제품 때문인지" 구분 불가능해진다. **판정 데이터가 나온 뒤 무드 개편 여부를 결정한다.** 그때의 선택지: ① 유지(클릭률 통과 시 — 무드는 문제가 아니었음) ② 파스텔 네일 무드로 전면 교체(미달 시 A/B 후보 1순위).

## I. 타이포그래피 (63~68)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 63 | 헤비 타이포 반복 | **보류** | 무드 개편 때 |
| 64 | Black Han Sans 자간 | **이미 해결** | `.xp-display` 등에 letter-spacing 보정 존재 |
| 65 | 흰 텍스트+섀도 의존 | **이미 해결** | `.xp-head-on-photo::before` 스크림이 실측 대비 기준으로 이미 구현됨 (섀도는 보조) |
| 66 | 줄길이 짧은 중앙 고정 | **보류** | 취향 수준 |
| 67 | Archivo Black 낭비 | **오진** | `--font-display`·`--font-latin`에서 라틴 글리프 담당 — 실사용 중 |
| 68 | 숫자 크기 대비 급격 | **보류** | 취향 수준 |

## J. 레이아웃·공간 (69~74)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 69 | 섹션 여백 불균일 | **보류** | `--space-section` 토큰 존재 — 밴드 리듬은 의도(Testimonials 주석). 무드 개편 때 |
| 70 | 순수 잔디 영역 과다 | **보류** | 무드 개편 때 |
| 71 | Stats 2×2 여백 과다 | **보류** | 취향 수준 |
| 72 | 갤러리 한 줄 고정 | **오진** | `auto-fit minmax(210px,1fr)` — 폭 따라 접힘 |
| 73 | FAQ 텍스트 벽 | **이미 해결** | Faq.tsx 주석에 비접이식 선택 근거 명시(답 짧고 개수 적음, 가짜 어포던스 제거) |
| 74 | 푸터 CTA 밀도 대비 비용 | **보류** | 무드 개편 때 |

## K. 색·대비 (75~79)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 75 | 하늘색 위 흰 텍스트 대비 | **이미 해결** | 65와 동일 — 실측 2.78:1 문제를 이미 스크림으로 해결한 이력이 CSS 주석에 있음 |
| 76 | 필 대비 편차 | **보류** | 토큰 일괄 점검은 무드 개편 때 |
| 77 | 잔디 위 오브젝트 대비 | **이미 해결** | `.xp-meadow::before` 섹션 스크림 + 국소 스크림 체계 |
| 78 | 다크 섹션 회색 본문 저대비 | **보류** | assurance 색 토큰 실측 후 — 27과 함께 |
| 79 | 포커스 링 | **이미 해결** | 전역 `:focus-visible` + 초원 배경 전용 변형 + clip-path 입력 예외까지 존재 |

## L. 모바일·반응형 (80~84)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 80 | CTA 2개 한 화면 겹침 | **보류** | 9와 동일 판단 |
| 81 | 390px 알약 폭 44% | **이미 해결** | TopBar 주석이 과거형("점유했다") — 스크롤 숨김으로 해결된 이력 기록 |
| 82 | 모바일 폴라로이드 기울기 | **수정** | 36과 함께 — 좁은 폭에서 tilt 0 |
| 83 | 손·SCROLL 겹침 | **이미 해결** | `.xp-hero-cue` z-index:2 + 배경 필 처리 존재 |
| 84 | '0원' 카드가 4번째 | **수정** | STATS 순서 변경: 0원을 2번째로 |

## M. 모션·인터랙션 (85~89)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 85 | '생성' 느낌 모션 부재 | **오진** | heroMorph가 정확히 그 역할(영감→구슬→손톱 재탄생 루프) |
| 86 | TopBar 깜빡임 | **이미 해결** | 8px 히스테리시스 + rAF 스로틀 |
| 87 | 호버 어포던스 | **이미 해결** | 폴라로이드 hover 존재, FAQ는 의도적 비클릭(73) |
| 88 | reveal 모션 저감 | **이미 해결** | landing.css에 `prefers-reduced-motion` 블록 2곳 |
| 89 | CTA 상태 피드백 | **보류** | 툴 화면 버튼 상태는 별도 감사 범위 |

## N. 접근성 (90~93)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 90 | alt 누락 | **오진** | 3과 동일 정정 |
| 91 | 영어 필 스크린리더 낭독 | **수정** | 장식 필 전체 `aria-hidden` (49와 동일 건) |
| 92 | FAQ 시맨틱 | **이미 해결** | 비아코디언이 설계 선택(73). h3+p 구조 적절 |
| 93 | 히어로 대체 텍스트 | **이미 해결** | `role=img` + 서사형 aria-label 존재 |

## O. 성능 (94~96)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 94 | 렌더 블로킹 폰트 CSS 3개 | **수정** | Google Fonts 2패밀리를 `next/font/google`로 셀프호스팅(요청 2개+preconnect 2개 제거). Pretendard 동적 서브셋은 CDN 유지(한글 서브셋 셀프호스팅 비용 > 이득) |
| 95 | preload 4개 | **이미 해결** | media 조건 분기 + reduced 분기 — 설계된 트레이드오프 |
| 96 | 초원 배경 next/image 미적용 | **보류** | preload+CSS 배경 조합이 이미 LCP 대응. 전환 이득 불확실 |

## P. SEO·신뢰 (97~100)

| # | 항목 | 판정 | 조치 |
|---|---|---|---|
| 97 | sitemap/robots 부재 | **수정** | `app/robots.ts` + `app/sitemap.ts` 추가 |
| 98 | FAQ JSON-LD 부재 | **수정** | Landing(서버 컴포넌트)에 FAQPage 구조화 데이터 삽입 — content.ts FAQS 단일 소스 재사용 |
| 99 | 문의 채널 부재 | **수정** | SiteFooter에 문의 메일 링크 추가 |
| 100 | privacy 일치 여부 | **이미 해결** | privacy 페이지 존재 + "코드를 바꾸면 이 문서도 바꿀 것" 규율 주석. 이메일 미저장 원칙도 PriceProbe와 일치 |

---

## 이번 커밋 실행 목록 (31건 → 파일 단위)

1. `app/layout.tsx` — SITE_URL 폴백(`VERCEL_PROJECT_PRODUCTION_URL`), next/font 전환 (#1, #94)
2. Vercel env — `NEXT_PUBLIC_SITE_URL` 등록 (#1)
3. `app/globals.css` — 폰트 변수 next/font 연결 (#94)
4. `components/story/HeroHand.tsx` — 게이트 분기 CTA, 스크롤 큐 한국어 (#7, #8, #21)
5. `components/landing/TopBar.tsx` — 게이트 분기 CTA (#8)
6. `components/landing/FooterCta.tsx` — 게이트 분기 + 구독 보조 링크 (#16)
7. `components/landing/PriceSection.tsx` — `id="subscribe"` (#12, #14)
8. `app/page.tsx` — 게이트 카드 탈출구 링크 (#10, #12, #32)
9. `components/PriceProbe.tsx` — 무료/유료 경계 한 줄 (#43)
10. `components/landing/content.ts` — STATS 순서, FAQ 보관함 답변 보강 (#84, #54)
11. 필 `aria-hidden` — Gallery·PriceSection·Stats·Services·Testimonials·Faq·FooterCta (#49, #91)
12. `components/landing/Landing.tsx` — FAQPage JSON-LD (#98)
13. `app/robots.ts`, `app/sitemap.ts` 신규 (#97)
14. `components/landing/SiteFooter.tsx` — 문의 링크 (#99)
15. `app/landing.css` — 모바일 폴라로이드 tilt 0 (#36, #82)

검증: `npm run typecheck` · `npm run test` · `npm run build` · 로컬 dev 화면 확인.

## 보류 항목의 다음 트리거

- **에셋 배치** (17~20, 34, 40): hand-after 재생성 + 착용샷 갤러리 교체 — 생성물 축적 시
- **무드 개편 판단** (6, 25, 29, 33, 52, 55~63, 66, 68~71, 74, 76): 검증 게이트(방문 200) 판정 후
- **카피 동결 해제** (22, 23, 47, 48, 50, 53): 동일 시점
- **기능 추가** (2, 37, 38, 39): B2C 트랙 존속 확정 후
